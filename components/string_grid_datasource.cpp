//---------------------------------------------------------------------------
#include "pch.h"
#pragma hdrstop

#include "string_grid_datasource.h"
//---------------------------------------------------------------------------
#pragma package(smart_init)
/*
//---------------------------------------------------------------------------
__fastcall TStringGridDatasource::TStringGridDatasource(System::Classes::TComponent* AOwner) : TStringGrid(AOwner) {
}

//---------------------------------------------------------------------------
void TStringGridDatasource::Build() {
	//size_t rows = datasource.data.size();
	datas
	if (rows == 0) {
		return Clear();
	}

	RowCount = FixedRows + rows;

	for (size_t row = 0; row < rows; ++row) {
		for (int col = 0; col < ColCount; ++col) {
            Cells[col][row] = datasource[0].Value(col).c_str();
        }
	}
}

//---------------------------------------------------------------------------
void TStringGridDatasource::Clear() {
	RowCount = FixedRows + 1;
	for (int col = 0; col < ColCount; ++col) {
		Cells[col][FixedRows] = "";
	}
}
*/
