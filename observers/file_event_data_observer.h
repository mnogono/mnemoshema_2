//---------------------------------------------------------------------------
#ifndef file_event_data_observerH
#define file_event_data_observerH

#include "request_file_event_data.h"
//---------------------------------------------------------------------------
/*
class TFileEventDataObserver : public sysObserver::IObserver {
	protected:
		std::map<const TRecord *, std::list<IMnemoshemaView *> *> *mshViews;
		std::map<const TRecord *, IMnemoshemaView *> *mshView;
	protected:
		void UpdateDataRecordViews(TTaskRequestFileEventData *task);
		void UpdateDataRecordView(TTaskRequestFileEventData *task);
	public:
		TFileEventDataObserver(
			std::map<const TRecord *, std::list<IMnemoshemaView *> *> &mshViews);

		TFileEventDataObserver(
			std::map<const TRecord *, IMnemoshemaView *> &mshView);

		virtual void UpdateData(sysObserverable::IObserverable *data = NULL);
};
*/
#endif
